import 'package:flutter/material.dart';

class AdminProBody extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [Expanded(child: Placeholder())],
    );
  }
}
